{
  "name": "contact-book-manager",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm run build:deps && pnpm run build:client && pnpm run build:protocol",
    "build:deps": "pnpm --filter @microsoft/webhub --filter @microsoft/webhub-framework --filter @microsoft/webhub-router run build",
    "build:protocol": "cargo run -p microsoft-webhub-cli -- build ./src --plugin=webhub --projection-manifest ./dist/webhub-projection.json --out ./dist",
    "build:client": "node ../../build-client.mjs",
    "start:client": "pnpm run build:deps && node ../../build-client.mjs --watch",
    "start:server": "cargo run -p microsoft-webhub-cli -- serve ./src --state ./data/state.json --theme=@microsoft/webhub-examples-theme --plugin=webhub --projection-manifest ./dist/webhub-projection.json --servedir ./dist --port 3003 --api-port 3013 --watch",
    "start:api": "esbuild server/api.ts --bundle --platform=node --format=esm --outfile=dist/api.js --packages=external && node dist/api.js",
    "start": "cargo xtask dev contact-book-manager",
    "test": "playwright test",
    "test:update-snapshots": "playwright test --update-snapshots"
  },
  "imports": {
    "#atoms/*": "./src/atoms/*",
    "#molecules/*": "./src/molecules/*",
    "#organisms/*": "./src/organisms/*",
    "#pages/*": "./src/pages/*",
    "#app/*": "./src/cb-app/*",
    "#api": "./src/api.ts"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@microsoft/webhub": "workspace:*",
    "@microsoft/webhub-examples-theme": "workspace:*",
    "@microsoft/webhub-framework": "workspace:*",
    "@microsoft/webhub-router": "workspace:*",
    "@playwright/test": "catalog:",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "esbuild": "catalog:",
    "tslib": "catalog:",
    "typescript": "catalog:"
  }
}

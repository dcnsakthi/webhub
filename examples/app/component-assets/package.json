{
  "name": "component-assets-example",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm run build:deps && pnpm run build:client && pnpm run build:protocol && pnpm run copy:data",
    "build:deps": "pnpm --filter @microsoft/webhub --filter @microsoft/webhub-framework run build",
    "build:protocol": "cargo run -p microsoft-webhub-cli -- build ./src --plugin=webhub --projection-manifest ./dist/webhub-projection.json --out ./dist --emit-component-assets lazy-panel --theme=@microsoft/webhub-examples-theme ",
    "build:client": "node ../../build-client.mjs",
    "copy:data": "node -e \"require('node:fs').copyFileSync('src/lazy-panel-data.json','dist/lazy-panel-data.json')\"",
    "start:client": "pnpm run build:deps && node ../../build-client.mjs --watch",
    "start:server": "cargo run -p microsoft-webhub-cli -- serve ./src --state ./data/state.json --theme=@microsoft/webhub-examples-theme --plugin=webhub --projection-manifest ./dist/webhub-projection.json --emit-component-assets lazy-panel --servedir ./dist --port 3010 --watch",
    "start": "cargo xtask dev component-assets",
    "test": "playwright test",
    "test:update-snapshots": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@microsoft/webhub": "workspace:*",
    "@microsoft/webhub-examples-theme": "workspace:*",
    "@microsoft/webhub-framework": "workspace:*",
    "@playwright/test": "catalog:",
    "esbuild": "catalog:",
    "tslib": "catalog:",
    "typescript": "catalog:"
  }
}

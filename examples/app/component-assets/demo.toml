# Demo shell metadata — auto-discovered by the demo shell server.
# See examples/demo/README.md for the full schema.

name = "Component Assets"
description = "A no-router app that loads a deferred component from static webhub assets"
backend = "rust"

[server]
type = "webhub-cli"
plugin = "webhub"
source = "src"
servedir = "dist"
state = "data/state.json"
theme = "@microsoft/webhub-examples-theme"

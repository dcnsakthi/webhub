{
  "name": "routes-example",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm run build:deps && pnpm run build:client && pnpm run build:protocol",
    "build:deps": "pnpm --filter @microsoft/webhub --filter @microsoft/webhub-framework --filter @microsoft/webhub-router run build",
    "build:protocol": "cargo run -p microsoft-webhub-cli -- build ./src --plugin=webhub --projection-manifest ./dist/webhub-projection.json --out ./dist",
    "build:client": "node ../../build-client.mjs",
    "start:client": "pnpm run build:deps && node ../../build-client.mjs --watch",
    "start:api": "cd server && pnpm start",
    "start:server": "cargo run -p microsoft-webhub-cli -- serve ./src --plugin=webhub --projection-manifest ./dist/webhub-projection.json --servedir ./dist --port 3008 --api-port 3018 --watch",
    "start": "cargo xtask dev routes",
    "test": "playwright test",
    "test:update-snapshots": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@microsoft/webhub": "workspace:*",
    "@microsoft/webhub-framework": "workspace:*",
    "@microsoft/webhub-router": "workspace:*",
    "@playwright/test": "catalog:",
    "esbuild": "catalog:",
    "tslib": "catalog:",
    "typescript": "catalog:"
  }
}

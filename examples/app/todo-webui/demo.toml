# Demo shell metadata — auto-discovered by the demo shell server.
# See examples/demo/README.md for the full schema.

name = "Todo (webhub Plugin)"
description = "A todo app using the webhub rendering plugin"
backend = "rust"

[server]
type = "webhub-cli"
plugin = "webhub"
source = "src"
servedir = "dist"
state = "data/state.json"

# Demo shell metadata — auto-discovered by the demo shell server.
# See examples/demo/README.md for the full schema.

name = "Calculator"
description = "A calculator with basic and scientific mode built on webhub Framework"
backend = "none"

[server]
type = "webhub-cli"
plugin = "webhub"
source = "src"
servedir = "dist"
state = "data/state.json"

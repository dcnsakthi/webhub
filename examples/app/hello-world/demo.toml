# Demo shell metadata — auto-discovered by the demo shell server.
# See examples/demo/README.md for the full schema.

name = "Hello World"
description = "A minimal webhub starter app with server-side state"
backend = "rust"

[server]
type = "webhub-cli"
plugin = "webhub"
source = "src"
servedir = "dist"
state = "data/state.json"
theme = "@microsoft/webhub-examples-theme"
